<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SudokuApp.ViewModels"
             xmlns:control="clr-namespace:SudokuApp.Controls"
             x:Class="SudokuApp.MainPage">
    <ContentPage.BindingContext>
        <vm:MainViewModel/>
    </ContentPage.BindingContext>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <control:SudokuControl/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Text="カメラ起動" Grid.Column="0" Command="{Binding ExecuteCameraCommand}"/>
            <Button Text="実行" Grid.Column="1" Command="{Binding ExecuteSolveCommand}"/>
        </Grid>
    </Grid>
</ContentPage>
